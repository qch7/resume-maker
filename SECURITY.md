# 安全说明

## 使用边界

Resume Maker 是单用户本机应用，仅监听 `127.0.0.1`，通过 Host、Origin 和每实例令牌保护本机文件接口。它没有多用户认证或公网部署能力，不应通过反向代理直接公开这些接口。

生产模型调用通过统一隐私网关发送脱敏文字。已知个人资料和常见敏感格式替换为请求内占位符，凭据直接移除；映射仅留在内存。模型仅能通过专用只读工具读取脱敏副本，无 shell、通用文件访问或旧供应商会话。图片及 PDF 先在本机提取文字；模板内嵌图片整图打上马赛克、清除元数据后可作为受控附件发送。图片模板和 PDF 扫描页的整页附件使用本地 OCR 重绘文字，敏感行不透明覆盖，非文字区域统一粗色块处理，不复制原始文字裁图或照片细节。原件不外发，处理失败时停止发送，禁止回退原图或开放图片读取工具。此工具沙箱为应用层边界，后端和 CLI 控制进程仍属于受信任代码。

规则不保证识别未知姓名、任意编码、上下文推断或全部商业秘密。请登记额外敏感值并检查发送记录。请求仍会发送给配置的供应商；CLI 的协议设置不能替代供应商自己的数据政策。本机数据库、原件和备份没有新增静态加密，旧版已发送内容无法撤回。详见 [隐私边界](docs/privacy.md)。

DOCX 导出仅操作登记区域。Windows 的 Word 预览会启动独立进程并禁用宏自动化执行，按进程身份回收；模板仍应来自可信来源。

## 本机系统日志

“系统日志”仅通过同一组本机认证接口提供，保存在数据目录的 `logs/activity.sqlite`。日志包含本机 AI 输入、回复、业务参数和工具结果，仍属于个人资料；鉴权头、Cookie、常见密钥及当前连接凭据在写入前遮盖，二进制文件只记录元信息。日志不会成为模型材料，也不会随资料 ZIP 备份发送；用户可以在日志区域主动导出筛选后的 JSONL。详情尺寸及保留策略见 [系统日志](docs/system-activity.md)。

## 报告问题

请勿在公开 Issue 中发布令牌、真实简历、源码秘密或可直接访问个人文件的详细利用信息。优先通过仓库的 [Security advisories](https://github.com/qch7/resume-maker/security/advisories) 页面报告；若私密报告入口不可用，请先创建不含敏感细节的 Issue，请维护者提供私密沟通方式。

报告应包含受影响版本、运行环境、最小重现和影响范围。当前维护对象为默认分支，不承诺旧版本长期安全支持。
