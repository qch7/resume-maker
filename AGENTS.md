# Resume Maker 开发约定

本文件适用于整个 `resume-maker` 仓库，供 coding agent 开始任务时读取；新增功能或调整规则后同步更新对应说明。

## 项目功能

Resume Maker 是单用户本机简历工作台，将项目源码整理成有引用依据的项目经历，再组合为可编辑的 Word 简历。

- 项目支持多个源码目录和独立子项目，本地采集有界源码文字，经隐私网关发送给无工具模型，分析后保存引用证据。
- 项目经历支持字段草稿、亮点排序、分支、历史恢复和不可变版本，每份简历引用明确的经历版本。
- AI 会话独立保存本机历史和输入，每轮脱敏重建上下文，不续用旧供应商会话，建议经用户采用后进入草稿。
- 生产模型出口仅允许脱敏的 Responses 文字请求，禁止原始图片、工具执行和未经处理的文件访问；新增功能必须经过同一隐私网关。
- 个人资料支持照片、联系方式、教育、课程、技能、自定义字段及两级栏目编排。
- 荣誉库支持证书上传、识别、人工核对、分类和排序，已核对资料可同步到多份简历。
- 模板库支持分类、收藏、改名、缩略图和回收站，可编辑 DOCX 和带文字层的 PDF 支持识别，依赖原始图片的恢复流程被生产隐私出口拦截。
- 模板适配支持字段映射、照片、重复条目、人工调整、缺项补齐和真实试填，保留原模板的样式及布局。
- 简历支持多个组合方案、实时预览、Word 导出、追溯清单和历史成品，PDF 及精确分页依赖 Windows 上的 Microsoft Word。
- 用户数据保存在本机 SQLite 和文件目录中，支持 ZIP 备份及离线恢复。

## 开始任务

- 先确认当前目录是本仓库，并检查 Git 分支和工作区状态，保留已有改动。
- 阅读 [README.md](README.md) 和对应模块；实现细节见 [架构说明](docs/architecture.md)，操作命令见 [开发指南](docs/development.md)。
- 修改范围围绕当前任务展开，发现无关问题时单独说明。
- 工作区存在其他任务的未提交改动时，使用独立 worktree 开发新功能，提交时只包含本任务文件。
- 优先沿用现有模块和依赖，新增抽象或依赖应解决明确的重复或实际需求。

## 技术栈和目录

后端使用 Python 3.12+、FastAPI、Pydantic 和 SQLite，前端使用 React、TypeScript 和 Vite，Node.js 要求 22.16+，Python 依赖由 uv 管理。

| 目录 | 主要内容 |
| --- | --- |
| `src/resume_maker/api/` | 应用工厂、依赖注入、HTTP 路由、请求模型和本机访问校验 |
| `src/resume_maker/core/` | 运行配置和业务异常 |
| `src/resume_maker/domain/` | 数据模型、经历规则、栏目结构和模板映射契约 |
| `src/resume_maker/services/` | 项目、草稿、修订、会话、荣誉、任务、模板库和导出业务 |
| `src/resume_maker/infrastructure/` | SQLite 访问、初始结构、实例锁、备份和恢复 |
| `src/resume_maker/integrations/` | 源码证据、隐私网关、模型连接、本机文件选择和文档处理 |
| `src/resume_maker/integrations/word/` | OOXML、模板填充、PDF 恢复、图片恢复和 Word 子进程 |
| `frontend/src/app/` | 跨功能协调、导航和工作台布局 |
| `frontend/src/features/` | 各功能的组件、状态和业务规则 |
| `frontend/src/shared/` | 通用控件、hooks、网络、本机缓存和 API 类型 |
| `frontend/src/styles/` | 按功能拆分的样式，导入顺序由 `index.css` 管理 |
| `tests/`、`frontend/tests/` | 后端回归测试和前端逻辑测试 |
| `scripts/` | 启停、检查、构建和模板评测脚本 |

后端依赖约束由 `scripts/check_quality.py` 检查：`core` 独立于业务模块，`domain` 不依赖存储或适配器，`infrastructure` 和 `integrations` 不导入 `services` 或 `api`，`services` 不导入 `api`。

前端 `shared` 不导入 `features` 或 `app`，`features` 不导入 `app`；通用草稿提交通过 `shared/lib/draftRegistry.ts` 协调。

## 注释规则

- 使用简短、自然的中文陈述句，尽量一句话说明用途、原因或必要条件，写不下时分成多行。
- 直接描述实际行为，删掉空泛提醒、重复解释和宣传式措辞。
- 避免“不是……而是……”或“并非……而是……”等对照式表达，并列内容使用顿号或“和”，不用“与”字串联。
- Python 函数保留简短中文 docstring，前端具名函数保留紧邻声明的中文说明，简单回调不重复解释代码。
- 注释末尾不加句号，减少逗号和分号堆叠，过长时先压缩表述再分行。
- 保留并发校验、事务、取消、文件路径和进程回收中的必要条件，修改行为时同步更新注释。

注释示例：

```python
"""根据父修订关系计算历史图坐标"""
```

```typescript
/** 等待当前 Word 请求完成后处理最新输入 */
```

## 业务规则

- 经历修订一经保存即不可变，发布和恢复通过新修订完成，历史关系使用真实父版本。
- 编辑、增删和排序先写草稿，整段提交才生成版本，内容没有变化时清理已确认草稿即可。
- 保存和删除校验草稿版本及分支头，并在同一事务中完成引用校验和写入，冲突时保留用户输入。
- 简历固定保存 `revision_id` 和 `highlight_ids`，经历更新后由用户选择是否用于当前简历。
- 单字段、单条经历和个人信息的保存各自保留其他未提交草稿，迟到响应不得覆盖后续输入。
- AI 结果先作为建议保存，采用前再次核对原文和当前状态，取消后的结果不得发布。
- 项目、经历分支、会话和应用实例分别隔离，来源分析只读关联目录并保存可追溯证据。
- 荣誉内容按稳定来源标识同步，各份简历保留自己的显隐、排序和备注，历史导出保持原样。
- 模板填充使用已确认的节点、精确引文和范围，未识别原文、照片缺位或栏目冲突需要明确报告。
- 模板补位、检查、试填和导出复用现有入口，在副本中处理并保留原文件，规则不得依赖某份简历的姓名、文件名或固定节点编号。
- Word 排版在独立子进程中执行，串行调用受全局状态影响的转换器，回收前核验进程身份。
- 当前项目尚未上线，数据库调整直接更新 `src/resume_maker/infrastructure/schema.sql` 的完整初始结构，并同步模型、接口和测试。

## 数据和运行环境

- 服务保持本机单用户运行方式，保留回环监听、Host、Origin 和实例令牌检查。
- 源码运行默认使用 `data/`，开发和验收使用临时目录，通过 `--data-dir` 或 `RESUME_MAKER_DATA_DIR` 指定。
- 测试使用合成资料、临时数据库和可控 Provider，真实供应商调用及真实资料发送以用户授权范围为准。
- 个人简历、证书、照片、数据库、聊天记录、密钥和生成产物不进入 Git 提交。
- 文件操作先核验路径范围和链接，保留原始源码及导入文件，清理只针对当前任务或当前实例拥有的产物。
- 详细约束见 [SECURITY.md](SECURITY.md)。

## 分支和 PR

1. 每个新功能从最新的 `main` 创建独立分支，默认使用 `codex/<简短功能名>`，用户指定名称时使用指定名称。
2. 在功能分支完成开发、检查和提交，禁止直接向 `main` 提交或推送功能改动。
3. 推送功能分支并创建以 `main` 为目标的 PR，说明问题、修改后的行为、验证结果和必要的数据结构变化。
4. 处理审查意见并等待 CI 通过，遵循仓库分支保护规则，再通过 PR 合并到 `main`。
5. 确认 PR 已合并且分支没有额外的未合入提交后，同步本地 `main`，删除该功能的远端分支和本地分支，再清理失效的远端跟踪引用。
6. 分支仍有未提交内容、额外提交或被其他 worktree 使用时先核对归属，清理范围只包含本次已合并的功能分支。

上述流程指源码仓库的 Git 分支，应用内的经历分支保存在 SQLite 中。

## 验证和交付

首次准备环境：

```sh
uv sync --locked
npm --prefix frontend ci
```

代码变更完成后运行完整检查：

```sh
uv run python scripts/check.py
```

该命令依次执行中文函数说明和依赖检查、Ruff、后端测试、前端类型和格式检查、前端测试及生产构建。

只修改文档时检查内容、链接和 `git diff --check` 即可；只改注释时确认可执行代码未变并完成格式及质量检查，已有通过结果可复用，发现相关问题后再补跑对应检查。

涉及打包或资源路径时额外运行：

```sh
uv build --wheel
uv run python scripts/check_wheel.py
```

- Python 使用 Ruff，TypeScript 和 CSS 使用仓库 Prettier 配置，依赖变更同步更新对应锁文件。
- 新功能和缺陷修复优先测试用户可观察的行为，避免添加逐行复述实现的测试。
- 排版改动检查实际 DOCX 或分页效果，自动测试中的 Word 替身不代表真实排版验收。
- 交付时简要说明改动和验证结果，未执行的检查说明原因，创建 PR 后附上链接，合并后说明分支清理情况。

更多协作要求见 [CONTRIBUTING.md](CONTRIBUTING.md)。
