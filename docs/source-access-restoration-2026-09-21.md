# 源码按需能力恢复（2026-09-21）

对照 `main` 的 `e3f6956`，原有分析按需读取关联目录，没有固定的整库文件数、总量和单文件大小预算。隐私分支引入的 200 文件、128000 字节、350000 字符及 10000 遍历项上限会漏掉合法源码；第一轮修复 `4191a88` 只改善多来源分配，仍未恢复这项能力。

## 当前实现

- 启动分析只登记来源编号，不提前扫描或收集正文。新增 `list_source_files`、`search_sources`、`read_source`，目录和搜索按游标继续，正文和长行按行列位置继续。
- 移除整库文件数、总字符量、单文件大小、遍历总数及 MCP 进程 500 条请求上限。单次结果及扫描步骤有界，空页通过 `complete` 明确是否结束。
- 原始目录和脱敏映射由受信任后端持有，独立 MCP 材料进程使用请求级鉴权回环通道。模型只得到脱敏结果；通道凭据不放入工具参数或模型上下文，禁止任意 shell、写入和通用文件读取。
- 每次读取复核来源、相对路径、链接和文件状态；实际应用数据目录及凭据仍排除。普通源码 `data` 目录、文本锁文件和 SVG 可用。
- 同轮共享隐私映射，新发现的身份可在最终引用中准确还原。文件变化或新敏感值使缓存重新生成；退出和取消清理网关、缓存及游标。审计仅保留最近 20 次工具结果和总调用次数，不限制读取次数。

## 回归范围

合成用例覆盖 230 个文件、多来源总量超过 350000 字符、超过 128 KB 的文件和长行、10000 次目录进度之后的文件、UTF-8/UTF-16 及 LF/CRLF/CR 换行、超过一页的搜索结果、缓存刷新、目录联接替换、硬链接、路径穿越和取消。

真实 Jobs → 隐私 Provider → 源码工具 → 证据核验流程从 206 个文件中取得大文件第 16001 行，验证脱敏文件名和新发现的邮箱在本机还原。原有多来源、来源重绑、最新文件、子项目隔离及按引用存档测试继续保留。

真实 Windows CLI 0.154.0 对本机模拟 Responses 服务覆盖普通模型、GPT-5.5、GPT-6-Astra：工具读取大文件末尾，搜索当前来源，返回动态占位符；shell、补丁、穿越、凭据及控制配置读取被拒绝。该验收使用合成资料，不连接真实供应商。

本地完整检查通过：698 项后端测试、125 项前端测试、中文说明和依赖检查、Ruff、类型和格式检查及生产构建。后端已显式启用三种模型的真实 CLI 验收。`uv build --wheel` 和 `scripts/check_wheel.py` 通过，仓库外安装包实际执行源码网关读取和脱敏还原，并验证前端、数据库和 OCR 资源。日志位于忽略目录 `output/source-access-check.log` 和 `output/source-access-wheel.log`。本次没有重复真实供应商或 Microsoft Word 视觉验收。

## 实际限制

单个被请求文件先完整脱敏后提供分页，所以极大文件仍受机器内存、处理时间和任务超时影响；取消在读取块及脱敏前后检查。自动隐私规则仍有识别边界，见[隐私保护](privacy.md)。本次恢复源码访问范围，不改变已记录的 OCR、图片和复杂版式限制。
