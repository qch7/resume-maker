# 初步版本开发记录

## 确认的范围

- 本机 Web，Python/FastAPI、React/TypeScript、SQLite。
- 项目分组的会话侧边栏，同一项目可新建多个独立会话。
- 本机 Codex 优先，Provider 接口允许后续增加 API Agent。
- 不可变经历版本、逐条人工编辑保存、可恢复草稿、AI 建议采用和冲突检查。
- 固定版本的简历组合、当前 Word 模板适配、可编辑 Word 导出。
- 保留模板其他栏目，只替换项目经历；允许多页并报告渲染页数。
- 源代码和个人数据分离，逐步开发、验证并提交 Git。

## 开发顺序

- [x] 1. 仓库、运行环境、数据模型与迁移。
- [x] 2. 项目扫描、输入快照、经历修订、草稿、组合。
- [x] 3. Codex Provider、持久化会话、队列、取消与错误恢复。
- [x] 4. Word 模板适配、导出和真实渲染验证。
- [x] 5. React 工作台、会话侧栏、逐条编辑、设置和导出。
- [x] 6. 集成验证、启动入口、文档和最终提交。

## 已检查的环境

- Windows，Python 3.12.7，Node 22.16.0，uv 0.10.11。
- Codex 0.154.0-alpha.6.2；exec 和 exec resume 均支持 JSON 事件及 output-schema。
- Word 已安装。LibreOffice 未安装。
- 用户通过 CCSwitch 配置中转 Provider，无需 CLI login；复用当前配置。
- 初始远端无引用；已有文件只有需求方案。

## 验证记录

随每个阶段补充实际运行结果，未执行的验收不得标为通过。

- 当前 CCSwitch 配置的真实 `codex exec` 结构化请求成功；以明确会话 ID 执行 resume 成功，并正确保留上轮内容。
- 核心数据/快照/任务首轮测试 9 项通过，涵盖不可变版本、单条保存保留其他草稿、重启读取、并发草稿冲突、项目归属、敏感值过滤、证据校验、会话隔离与取消。
- 后端扩展为 18 项通过，增加 API 同源/令牌保护、模板区域外包内容不变、草稿排序、历史恢复来源、ZIP 路径校验、恢复前保留旧数据及单实例锁。
- `syncraft` 与 `mo-bao` 实际源码分析成功，各生成 3 条亮点，分别有 10 / 12 条原文匹配的源码证据；两项目使用不同 Codex 会话 ID。syncraft 后续消息以原 ID 续聊成功，正确保留本项目上下文。
- 真实结果留在正式数据中的待采用建议；浏览器验收在独立目录完成采用、版本保存和组合，不把验收编辑写入正式经历。
- Playwright 浏览器完成项目手动导入、逐条创建 r2–r4、r5 单项保存、第二项草稿跨刷新保留，以及简历仍固定 r4 的验证。
- 浏览器完成真实 AI 建议采用、两项目组合、Word 导出、3 页实际预览和下载。Word 渲染逐页检查完成；照片、个人资料、教育背景和专业技能保留。
- 原模板和导出结果均另行通过 packaged `render_docx.py` 渲染。LibreOffice 对原模板已有浮动文本框显示差异（如顶部英文与年龄换行），相同差异也出现在未修改的原模板；产品使用 Microsoft Word 精确渲染。
- 修复 Word COM HWND 归属查询，仅清理本次创建且 PID/创建时间匹配的 Word 实例。备份恢复测试修复 Windows SQLite 连接未释放造成的文件占用。
- 浏览器完成会话改名、两条输入草稿切换/刷新保留、归档恢复；两窗口并发写入返回冲突，页面刷新后仍拒绝旧草稿覆盖，用户可显式载入服务器内容。
- 浏览器验证 390px 窄屏无横向溢出、暗色模式、完整备份下载；下载的 ZIP 通过 CLI 恢复成功，包含原会话输入草稿。
- `start.ps1` 首次启动、已有实例复用，`stop.ps1` 实例身份核对和正常关闭均已实测；个人数据统一使用用户主目录下 `.resume-maker`，避免 Windows 应用虚拟化导致多个数据位置。
- 对真实模板按登记的替换范围（零基序号 17 到 44，不含 44）逐节点比较：区域外正文节点逐字节一致，所有其他 DOCX 包文件逐字节一致。实际范围以每次模板导入记录为准。
- 浏览器本地恢复草稿携带服务器版本号，刷新不能绕过并发冲突检查；载入服务器草稿前保留本地恢复副本。保存组合出现版本冲突时可显式载入服务器组合。
- 重开简历会读取最近一次导出；已验证保存的导出接口返回对应简历与实际 3 页记录。
- 最终 Python 测试 18 项通过，Ruff 检查/格式检查、TypeScript 与 Vite 构建、Prettier 检查通过。测试依赖存在两条上游弃用提示，不影响运行结果。

- 2026-09-12：修复整段保存入口随 AI 草稿拆分而消失的问题。“保存全部修改”固定显示于版本选择旁，逐字段草稿也可一次保存；无修改时明确提示。前端构建及差异检查通过，浏览器确认 TrustGuard 的逐字段草稿状态下按钮可见，验证未发布用户草稿或更新简历组合。

- 2026-09-12：增加浅色、深色、跟随系统主题，首屏读取偏好并随系统实时切换；统一控件、状态提示和滚动条配色，内容预览保留白纸样式。增加可跳转的四步制作指引、版本引用提示、已选数量与旧导出提醒。
- 前端流程测试 8 项通过，覆盖草稿/本地编辑、固定版本更新、多项目空白经历、模板、组合变化和导出状态。Playwright 验证主题覆盖与刷新记忆、系统主题实时变化、四步导航定位，以及 1280/980/760/390px 无横向溢出；390px 默认收起侧栏。浏览器模拟草稿写入验证编辑后进度回退、自动存草稿不等于发布版本，拦截所有测试写入，未修改用户正式数据。前端构建、格式与差异检查通过，浏览器无控制台错误。

- 2026-09-12：工作台改为可调分栏，项目栏、编辑区和简历区可横向分配空间，制作指引与右侧设置/预览可纵向调整；窄屏增加编辑区高度调节。分隔条支持指针拖动、键盘、双击复位和布局记忆；窗口缩小仅约束显示尺寸，不覆盖原偏好。增加全局重置、收起指引和放大预览（Esc 恢复），项目标题工具栏压缩至约 50px。
- 布局边界/恢复与原流程测试共 12 项通过。Playwright 实测各分隔条拖动、键盘边界、刷新记忆、双击/全局复位、指引折叠、嵌套滚动后的步骤导航、桌面与手机放大预览，以及 981/980/760/390px 布局；验证时拦截所有非 GET 数据请求。前端构建与格式检查通过，浏览器无控制台错误。

- 2026-09-12：侧栏增加铃铛（最近修改）和 A–Z（正序/倒序）排序，项目与项目内会话同步排序并记忆偏好。项目活动时间聚合版本、草稿与未归档会话；会话改名和输入草稿修改同步更新时间，相同内容重复提交不刷新时间。
- 后端 19 项、前端 15 项测试通过，Ruff、Prettier 与前端构建通过。浏览器验证连续切换、刷新恢复、最窄侧栏显示、模拟轮询后自动调整顺序，以及保留当前编辑项目；测试拦截写入，未修改正式数据。使用项目停止脚本与 CLI 重启现有本机服务，已验证新版活动时间字段生效。

## 当前边界

- 完成 Codex Provider；自定义 API Agent 属于后续接入工作。
- 当前 Word 适配器支持正文段落项目区，精确渲染依赖 Windows Microsoft Word。
- 原模板分页和分节保留，不自动压缩字号或改动其他栏目来强行减少页数。
- 源码快照是分析输入材料，具有明确大小/数量限制，不承担完整源码备份用途。
