# 更新记录

## Unreleased

### 改进

- 取消 Codex CLI 固定为 0.154.0 的版本限制，设置检查和实际调用统一接受其他版本及预发布版本；严格配置、受控模型目录、脱敏和只读工具边界继续生效，不兼容时返回具体错误。
- 思考强度改为可输入的开放标识，支持 `none` 和 CLI 后续新增的值，保存、功能覆盖及配置继承使用同一规则。Profile 支持空格、点号和中文名称，内嵌配置按完整键读取，独立配置文件仍限于 CLI home 内。

- 扫描 PDF 和原生转换失败的页面接入本地 OCR、敏感文字遮盖、图像打码及 AI 整页版面恢复，原文和照片在本机还原；混合 PDF 保留每页尺寸、顺序和可编辑内容。重新识别时继续保护各扫描页身份和不确定文字，失败或取消保留旧结果；旧 OCR 模板需从原 PDF 重新识别。
- 修复加入隐私保护后图片模板退化为仅 OCR、丢失照片和装饰的问题：本地 OCR 后遮盖敏感行，图像区域打码，发送脱敏重绘的整页图和坐标供 AI 恢复版面；原文和照片在本机还原，失败不回退原图。修复等线字体把半角连字符映射成其他横线而误报缺字的问题，继续检查原文完整性。

- 模板识别和 AI 修复新增本机马赛克图片通道：整张内嵌图片降为粗色块并清除元数据，添加节点编号后供模型判断照片用途。文字继续脱敏、原图留在本机；处理失败不回退原图，未展示或仍无法辨认的图片需人工确认。

- 修复证书名称被当成人名登记后造成跨请求过度脱敏：按个人、教育和荣誉字段语义识别身份，保留证书和项目名称、专业、职位及成绩；姓名、学校、编号、凭据和显式敏感词继续保护。隐私设置拆成独立页面，以敏感词和预览为主，规则和发送记录按需展开，切换页签保留未保存输入。

- 修复证书长编号仅局部遮盖的问题：完整替换 12 位及以上数字、分组编号、校验串和明确标签的编号，覆盖 OCR 空白、换行、全角及控制符；合并重叠替换范围，防止短敏感词截断编号。纯数字输入同样脱敏，日期、成绩、源码变量及原始行号继续保留，结果仍在本机准确还原。

- 恢复源码按需分析能力：移除隐私分支新增的整库文件数、正文总量、单文件大小和遍历预算，支持可继续的目录、搜索及长行读取；普通项目 `data` 目录、文本锁文件和 SVG 保持可用。原件由本机网关读取并脱敏，模型仍无 shell 和通用文件访问权限，引用在本地还原核对。

- 隐私分支回归修复输出契约被敏感词改坏、多来源源码采集失衡、原生 PDF 被强制 OCR 和材料工具截断 JSON 的问题；长行可分段读取，原生 PDF 继续使用本地结构解析，动态栏目和 OCR 身份仍脱敏。新增合成回归验证原有能力及隐私边界。

- 完整 Windows 验收修复证书学校、颁发单位、换行标签及 OCR 全角空白变体漏检；通过受控模型目录关闭内置模型重新启用的工具协议和补丁能力。CLI 挂起创建后加入 Job Object，避免快速退出竞态；脱敏源码保留 Windows 原始行号。验收覆盖真实供应商、浏览器、Word/PDF、模板、证书和备份恢复，见[验收记录](docs/privacy-acceptance-2026-09-21.md)。

- 所有生产 AI 入口经过本机脱敏网关，随机占位符在本机还原；新增敏感词、本地检测和材料审计。保留 Windows 原生 Codex CLI 0.154.0，通过只读材料工具访问脱敏副本，关闭 shell 和外部工具，无需 WSL。图片和 PDF 使用 RapidOCR/PP-OCRv4 移动版本地预处理，可靠文字层直接提取，难页最多提高分辨率复核一次。支持文件保存的订阅登录和 API 配置。规则及 OCR 仍可能漏检，详见[隐私边界](docs/privacy.md)和[OCR 评测](docs/ocr-benchmark.md)。

- 重复样本中同一非亮点字段的多次绑定会在识别、缓存读取与导出时被拒绝，避免把多条旧记录误当成一条后重复填写；修正请求重附出错节点的准确原文，保留精确引文校验。跨模型验收新增记录出现次数检查，区分用户填写的同值记录与程序多填。
- 模板 JSON 格式错误进入同一套最多三轮的字段反馈修正；提示正文携带真实输出契约，兼容带简短说明的唯一完整 JSON 代码块，但仍拒绝多个结果、截断内容和非法节点。项目标题别名统一使用实际栏目名。
- PDF 恢复保留物理行边界；双标题、连续留白与单侧后续标题共同证明的独立侧栏恢复为两个连续容器，避免记录被横向拆散；真实表格线及普通表头不触发该规则。照片使用独立锚点，删除旧摘要不会牵连照片；删除完整父块时同步清理其固定装饰保留项，动态字段冲突仍拒绝。
- 新增独立合成的六种中英文 DOCX/PDF 版式与跨供应商评测，校验旧样例残留、姓名重复、照片替换和可见资料；补充改名、打散节点、记录增减、隐藏栏目及侧栏反例回归。
- 模板识别由程序补齐缺失的个人信息、项目字段和普通子栏目；无标题的空白重复区自动重建为完整栏目，避免页首资料随荣誉等栏目移动。首次导入时分离正文与浮动标题的共用锚点，并校验固定标题不能被重复复制。
- 模板结构化输出限定真实节点与栏目候选，自动合并重复的父子删除项；Codex 调用兼容完整 JSON 代码块，并正确处理重连后成功返回。PDF 页首先应用已确认删除项，紧贴的日期与名称自动补充分隔。
- 新增隔离供应商的真实模板评测脚本，保存每轮输入、映射、Word/PDF 预览与内容检查；CC Switch 配置只读，认证仅传给测试子进程。

- 项目库新增删除按钮和二次确认；被任一已保存简历或当前简历草稿使用的项目不能删除，并提示占用的简历。删除整体项目会一并删除子项目，源码目录和历史导出文件保留；运行中的 AI 任务需先完成或取消，删除后自动切换到剩余项目或空状态。

- Word 适配层按模板、PDF 和图片拆分；模板服务与默认栏目设置归入独立目录；更新全部模块引用并使用包资源读取映射说明
- 移除旧元信息草稿的显隐迁移包装及其专用参数；保留当前草稿恢复、并发检查、版本引用与显示设置
- 精简代码注释并统一末尾标点；具名函数保留职责说明；简单回调不再要求重复注释

- 默认栏目设置按当前简历的栏目编排顺序展示，子栏目紧跟父栏目；新增栏目和信息项搜索，点击结果或按回车可跳转并聚焦对应字段。

- 修复默认设置给旧简历重复补入同名栏目的问题：唯一同名同类型栏目沿用原标识，保留父子关联；已有草稿只清理自动补入的同名空栏目，保留填写内容，并恢复可确认的原有父级。

- 栏目编排顶部新增“添加大栏目”和“默认栏目设置”入口。可集中编辑个人信息、教育、项目、荣誉和通用栏目的默认字段，设置保存到本机数据库并供新简历沿用；当前资料保留原值，默认项在填写页仅可隐藏。删除已填写的默认项先二次确认，移除后原值保留并隐藏，取消或保存失败不应用设置。

- 制作指引可向上拖动到单行最简模式：先隐藏说明，再隐藏子步骤，始终保留六步导航。桌面与窄屏共用 40 像素内容高度下限，继续向下拖动可恢复完整指引，并记住尺寸偏好。

- 制作指引扩展为模板识别、个人信息、项目经历、荣誉证书、组合编排、简历导出六个步骤，统一使用四字名称并在各工作区持续显示。补充资料、项目与荣誉子步骤，支持跨步骤跳转和表单定位；按整份简历计算就绪进度，兼容内置模板，保留项目版本与导出过期提醒，并适配窄屏和折叠布局。

- 图片模板使用独立的空间恢复协议，保留文字行、同行字段、分栏、字体颜色、图标与头像位置；含文字的标题底块独立重绘，防止旧标题残留。统一图片比例、旋转方向及透明底，填入真实资料时自动安排顶部字段、照片及长标题。文字重叠、裁图覆盖正文等错误有界重试；Word、扫描 Word 和 PDF 原入口保持不变，旧图片模板需从原图重新识别。
- PDF 模板试填时按实际资料宽度重排顶部标题、联系方式和新增字段；图标随字段显隐，照片独立留位。新增来源坐标关联、长标题底块扩展与占用高度校验，兼容本程序已恢复的 PDF 模板，原生 Word 排版和缓存契约不变。
- PDF 模板使用独立的逐页恢复流程：原生文字保留段落、同行对齐、表格和分栏，图标、照片及标题底色独立保留并跟随文字移动；扫描页或转换失败时明确回退视觉识别。覆盖字体图标、细分隔线、多行列表、背景层级、混合页、旋转与裁切、空白页、超链接、取消和原文完整性校验。原生 Word 识别及旧模板快照不变，旧 PDF 模板需从源文件重新导入。

- 模板库支持在详情标题旁重命名导入模板，保持映射、文件及简历引用不变；使用提示简化为单行“几份简历使用中”，移除重复的首页和选择说明。

- Word 模板的“保存识别结果”与“用于当前简历”拆为独立操作；保存只登记模板库，未保存的修改不能应用旧版本，刷新后仍可重开刚保存的结果。个人主页等联系方式被识别到资料区前后无标签留白时，自动补齐标签并继承联系方式样式，保留已有标签和图标的明确位置。

- 新增个人资料优先沿用完整联系方式的标签和值样式，避免复制孤立城市值的狭窄缩进；旧版自动补位在副本中同步修复。重复条目中用空格推开的名称与日期改为独立列宽，长名称自动换行，窄容器统一分行；按样本原位置读取分节宽度，预览与导出共用规则。

- 项目分析和单条 AI 修改统一使用亮点写作规则：默认精选 3–4 条，标题聚焦单一能力，正文一句话、分句用逗号，保留格式支持、处理策略、关键技术等有效细节，压缩重复措辞而非关键事实，并优先呈现有来源的量化数据。机制名称按含义汉化，包括事务消息表和分步失败补偿，具体技术栈及常用缩写保留。

- 未提交状态按当前内容与正式版本的实际差异判断；文字或排序改回原样后立即恢复“内容已提交”，同步清除待提交提示、撤销入口与制作指引阻塞，不生成多余版本。

- 未提交改动状态旁新增“撤销改动”，二次确认后原子清除当前基线的内容与排序草稿，保留显隐及其他版本；发生并发修改时完整保留草稿。移除编辑区重复的草稿说明横幅。

- 项目基本信息的标题和参与时间固定置顶，其余字段支持拖动及上下箭头排序；新增“项目亮点”占位栏，可整体移动亮点组，组内仍在下方编辑。基本信息顺序随项目草稿提交为新版本，预览、内置 Word 及已映射模板同步采用；显隐仍只保存到当前简历。

- 项目经历的基本信息沿用个人信息的紧凑编辑布局，支持添加自定义条目。基本信息与项目亮点可直接点击眼睛切换显示，显隐独立保存到当前简历，不要求提交项目新版本；兼容原有显隐草稿，预览与 Word 导出一致。

- 荣誉库核对窗口移除标题副文案、底部说明与重复关闭按钮，“确认并保存”集中至右上角；已核对条目不再展示历史识别提示，新结果填入表单后即时收起提示，保存失败在顶部显示原因。

- 精简荣誉条目编辑弹窗，移除标题说明和重复识别结果提示，取消“仅显示名称和日期”快捷按钮；“添加信息”移至“其他荣誉信息”区域，逐项显隐与保存方式保持一致。荣誉库保留识别结果核对与采用入口。

- 荣誉卡片加入简历后切换为“从简历中移除”；栏目编排中的荣誉条目新增左侧铅笔编辑与右侧 × 移除，沿用统一编辑弹窗。移除仅影响当前简历，库中资料、原件和其他简历保留，支持再次加入。

- 简历库的保存状态、项目与亮点数量及删除入口集中到左侧方案卡片，移除右侧重复标题；删除其他方案时保留当前选择和草稿。

- 简历管理弹窗简化为“简历库”，移除重复说明与底部返回栏，将名称、模板和保存／导出集中为紧凑操作行，为导出记录留出更多空间。

- 顶部新增统一“导出与模板”入口，打开与模板库一致的简历成品管理弹窗，集中搜索和切换方案、新建与删除简历、选择模板、保存和导出；右侧完整空间用于实时预览。导出记录显示历次成品的首页、时间和页数，支持下载 Word、可用的 PDF 与版本清单，并区分历史文件和当前内容。

- 个人信息中的荣誉栏目使用“添加证书”进入荣誉库，新增“排序”入口并定位到对应的栏目编排；移除荣誉条目上重复的上下移动按钮，页面切换保留当前简历草稿。

- 栏目编排内的荣誉、教育及自定义资料支持与项目经历一致的拖动、键盘和上下箭头排序，顺序同步至个人信息、预览与导出；保留隐藏条目及其完整内容。荣誉卡片以名称作为标题，统一简化字段标签。

- 荣誉内容与库中同一记录保持同步，已有缺字段条目自动补齐；个人信息合并为条目顶部一个“编辑”入口，在同一表单编辑内容与显示设置，移除重复提示。预览和导出使用最新核对内容，同时保留各份简历的显隐、排序、自定义备注及其他未保存草稿。删除来源保留最后核对内容，历史导出不变。

- 荣誉库与个人信息共用证书名称、奖项、级别、颁发单位、日期、获奖人、编号、分类和说明字段，加入简历时完整保留、逐项对应；新条目默认仅显示名称和日期，其余资料折叠并可独立显示，已有条目支持一键采用相同展示。

- 重复条目的标题与同列正文按有效样式自动对齐，新增元信息共用同一缩进并禁用继承编号；隐藏普通字段时一并收起其独占标签、列表段落和空表格行。规则支持样式继承及不同表格结构，保留多栏、独立单元格、右对齐日期和装饰图形。

- 大栏目默认紧接前一个栏目连续排版，取消原模板的强制分页、段前分页和另起页分节；保留标题装饰和原有分栏，内容超出当前页时自然换页。

- 已保存 Word 模板支持自动追加实习经历等大栏目，复制现有大标题的完整图形、底纹和文字样式，并按栏目编排排序；普通条目新增标题、补充信息、时间或正文时自动补充位置，空的新增字段不占行，无需重新识别。

- 隐藏个人信息时同时移除标签、值及空行；多列联系方式与普通表格中的后续条目按原列向上补齐，预览和导出保持一致。

- 已保存 Word 模板的个人基本信息区可自动扩展：新增个人主页等字段或自定义信息时，预览和导出沿用原有字体、缩进与行距补行，无需重新识别；隐藏或清空后不留下新增空行，模板原文件与映射保持不变。

- 新增独立“荣誉证书”管理板块，支持 PDF 和常见图片批量上传、视觉识别、原件核对、分类搜索和手动录入；已核对荣誉可加入不同简历并自动去重，原件随备份保存。支持取消、重试、并发编辑检查及证书识别模型单独配置。

- 所有模板选择入口统一为大尺寸二级弹窗，采用文件资源管理器式分类栏、搜索和名称/日期排序；支持列表与真实首页预览卡片切换、新建与删除分类、移动模板及 Like 收藏。分类和收藏保存在本机数据库，缩略图按内容缓存，取消浏览不改变原选择。

- 将精细调整合入“Word 试填与调整”，左侧保留真实分页，右侧按资料和栏目修正识别；完整原文、空白位置和范围工具按需展开。修改后保留上次效果并提示更新，更新试填前禁止保存和下载旧结果。

- Word 模板库与导出排版共用模板选项，补齐“内置 · 完整简历”的预览和选用入口；进入模板页默认显示当前简历版式，内置预览跟随当前资料与经历工作副本更新。
- 项目分析与对话改为直接只读关联目录，移除整库快照采集及文件数量、总量、单文件大小限制；每轮读取当前源码，模型返回后仅保留引用文件用于证据核对，旧快照继续供历史追溯。

- 模板导入自动整理动态域、修订、绑定、锁定与失效引用；支持旧版 Word、PDF 和图片，复杂对象与扫描内容逐页恢复为可编辑文字和独立照片。识别后自动试填检查，遇到栏目容器冲突时重建并重新映射，页面恢复失败自动重试。新文件识别失败时单独显示文件名，并明确下方已有模板结果的归属。
- 右上角 Codex 设置支持全局默认模型与思考强度，并为项目分析、普通对话、亮点修改、模板识别、模板完善及连接测试分别覆盖；空值继承，保存后用于新提交任务。模板每轮修正使用选定强度，切换模板模型或强度后重新识别。
- 模板保存时保留识别动态、轮次、耗时与用量，重启后重开或人工另存仍可查看；未留存记录的模板不再显示误导性的 0 秒。Word 模板页采用紧凑状态栏，左右分栏、记录区、AI 输入区和窄屏预览均可拖动调整，尺寸自动记忆并支持复位。
- 已保存模板选中后自动加载映射与 Word 试填预览，进入模板页时加载默认选项；支持读取失败重试，切换模板丢弃过时结果，调整映射保留当前编辑。
- 精简 Word 模板工作区：移除重复标题、介绍和步骤卡片，导入、AI 识别与模板选择集中到紧凑操作栏，为识别结果留出更多空间。
- Word 模板统一通过 AI 识别完整简历，移除手动项目区入口、段落导入接口及对应填充代码；保留识别结果的人工调整、真实试填和模板复用。模板库仅列出完整映射，原文件和历史导出保留。
- 选中 Word 模板后，右侧默认显示同一填充器生成的真实模板分页，随未保存资料和经历工作副本自动更新；连续输入合并排版，相同内容复用预览，切换模板丢弃过时结果。内置版式和导入模板统一为一个真实预览，采用矢量分页并支持缩放；项目管理放入“栏目编排”的项目经历栏目，预览完成后直接展示页面，历史导出保留文件下载。模板预览和导出同步采用当前栏目顺序与层级，标题与正文一起移动；处理浮动标题错位及空栏目残留标签。排版失败可重试并下载临时 Word。
- 模板识别压缩重复清单，自动修正续用独立 Codex 会话，仅补发校验反馈；相同模板、资料结构及 AI 配置复用已检查通过的结果，新增字段、模板变化或人工修正仍重新分析。
- Word 模板页展示 Codex 公开活动、轮次、实际耗时和累计用量；增量进度接口避免反复传输模板正文，支持断线重连和取消后冻结。
- 模板映射经验沉淀为随安装包分发的 `resume-template-mapping` skill，首轮直接加载，后续修正复用。

- 完整模板识别增加自动校验和最多三轮 AI 修正，支持文字说明继续调整；修正失败或退步时保留已有最佳建议。
- 模板页默认展示资料与栏目摘要，检查通过后自动试填；精细段落和范围编辑按需打开，人工调整后自动检查。
- AI 同时接收带节点标识的模板图片，支持识别证件照；批注在副本中自动忽略，脚注尾注参与映射，重复文字表示不再重复统计，连续分节与分栏在复制和删除条目时保留。
- 修复校验遇到首处错误就停止、导致后续已识别内容全部显示待处理的问题。
- 所有本机路径输入新增 Windows 原生文件 / 文件夹选择按钮，覆盖模板、项目集合、来源重绑与 CLI 程序，支持追加目录、去重和取消保留原值。
- 新增顶部独立“Word 模板”板块，位于“栏目编排”右侧；支持原文分色、字段高亮、选区人工调整、重复范围选择及已保存模板重开另存。
- 新增 AI 完整 Word 模板适配：自动建议个人资料、照片和重复栏目的位置，支持映射核对、未识别内容处理及真实试填；保存后随资料编辑继续沿用模板。
- 完整模板覆盖正文、表格、文本框、页眉页脚和内嵌照片，按实际记录增减教育及项目示例；遗漏当前资料时阻止导出，清理不再使用的旧照片资源。
- 源码运行默认将数据库及配套资源保存到项目 `data/`，启停脚本使用同一目录；独立安装包保留用户目录默认值，并继续支持显式覆盖。
- 增加项目经历、个人信息、栏目编排与 Word 模板四个主要功能区，支持固定顶部资料、照片、多条教育经历与自定义资料栏目。
- 支持大栏目与子栏目、拖动和箭头排序、显隐、重命名与删除撤销；主修课程默认归入教育背景。
- 各条栏目资料支持独立编辑和保存，成功后回到只读，并保留其他未保存草稿。
- 内置完整简历 Word 排版，个人信息与栏目随方案保存、备份和导出；支持 AI 完整模板替换。

- 按 API、领域、业务服务、基础设施和外部适配拆分后端，应用实例通过请求依赖注入隔离。
- 按业务组织前端，提取侧栏、单条经历编辑、AI 建议、任务进度、简历组合和布局管理。
- 为函数、回调与关键逻辑补充中文说明，加入自动注释和依赖边界检查。
- 提供跨平台 CI、HTTP 契约回归、实例隔离和生命周期验证，以及包含前端资源的 wheel 构建。
- 补充架构、开发、贡献、安全说明及 MIT 许可证，历史验收记录归档。

### 开发结构精简

- 数据库统一为 `schema.sql` 一次建库，当前结构版本为 6；删除历史增量迁移与启动补建逻辑。启动与恢复只接受当前结构，不转换或清空旧数据。
- 经历统一整段提交，移除单字段发布及剩余草稿搬运；主分支头版本直接查询，避免重复存储。
- 删除停用的个人资料删除标记与旧字段补齐，简化亮点顺序整理，统一当前资料结构与保存响应处理。浏览器简历草稿改用 `rm.resume.v2.*` 键。
- 保留逐字段草稿、分支历史、固定经历引用、栏目资料独立保存、模板和完整简历导出，以及当前结构的备份恢复。

## 0.1.0 — 2026-09-12

- 首个本机项目经历工作台，支持来源快照、独立 Codex 会话、版本与草稿、Word 模板导出、备份恢复。
- 增加主题、制作指引、可调布局、侧栏排序、亮点/项目拖拽排序和聊天输入区调整。
