# 更新记录

## Unreleased

### 改进

- 新增顶部独立“Word 模板”板块，位于“栏目编排”右侧；支持原文分色、字段高亮、选区人工调整、重复范围选择及已保存模板重开另存。
- 新增 AI 完整 Word 模板适配：自动建议个人资料、照片和重复栏目的位置，支持映射核对、未识别内容处理及真实试填；保存后随资料编辑继续沿用模板。
- 完整模板覆盖正文、表格、文本框、页眉页脚和内嵌照片，按实际记录增减教育及项目示例；遗漏当前资料时阻止导出，清理不再使用的旧照片资源。保留手动项目区模板模式。
- 源码运行默认将数据库及配套资源保存到项目 `data/`，启停脚本使用同一目录；独立安装包保留用户目录默认值，并继续支持显式覆盖。
- 增加项目经历、个人信息、栏目编排与 Word 模板四个主要功能区，支持固定顶部资料、照片、多条教育经历与自定义资料栏目。
- 支持大栏目与子栏目、拖动和箭头排序、显隐、重命名与删除撤销；主修课程默认归入教育背景。
- 各条栏目资料支持独立编辑和保存，成功后回到只读，并保留其他未保存草稿。
- 内置完整简历 Word 排版，个人信息与栏目随方案保存、备份和导出；支持模板项目区替换。

- 按 API、领域、业务服务、基础设施和外部适配拆分后端，应用实例通过请求依赖注入隔离。
- 按业务组织前端，提取侧栏、单条经历编辑、AI 建议、任务进度、简历组合和布局管理。
- 为函数、回调与关键逻辑补充中文说明，加入自动注释和依赖边界检查。
- 提供跨平台 CI、HTTP 契约回归、实例隔离和生命周期验证，以及包含前端资源的 wheel 构建。
- 补充架构、开发、贡献、安全说明及 MIT 许可证，历史验收记录归档。

### 开发结构精简

- 数据库统一为 `schema.sql` 一次建库，当前结构版本为 6；删除历史增量迁移与启动补建逻辑。启动与恢复只接受当前结构，不转换或清空旧数据。
- 经历统一整段提交，移除单字段发布及剩余草稿搬运；主分支头版本直接查询，避免重复存储。
- 删除停用的个人资料删除标记与旧字段补齐，简化亮点顺序整理，统一当前资料结构与保存响应处理。浏览器简历草稿改用 `rm.resume.v2.*` 键。
- 保留逐字段草稿、分支历史、固定经历引用、栏目资料独立保存、模板和完整简历导出，以及当前结构的备份恢复。

## 0.1.0 — 2026-09-12

- 首个本机项目经历工作台，支持来源快照、独立 Codex 会话、版本与草稿、Word 模板导出、备份恢复。
- 增加主题、制作指引、可调布局、侧栏排序、亮点/项目拖拽排序和聊天输入区调整。
